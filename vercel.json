{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pip install -r requirements.txt && python src/manage.py migrate --noinput",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
